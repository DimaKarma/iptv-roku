# Copy this to tools/secret-needles.txt and fill in the real values.
# That copy is gitignored: it holds the secrets themselves, and a scanner that
# commits what it hunts for is worse than no scanner.
#
# One "name = value" per line. Blank lines and # comments are ignored. An empty
# value is skipped, so an entry you have no value for costs nothing.
#
# The name is what the report prints, and it is what the ALLOWED table in
# check_secrets.py keys on together with a path - so renaming an entry here
# silently un-allowlists its known-acceptable matches, and they start blocking.
#
# The playlist URL, the provider host and the subscription token are NOT listed
# here. They are read from config.json, which is where they already live.
roku dev password =
tv lan ip =
owner email a =
owner email b =
